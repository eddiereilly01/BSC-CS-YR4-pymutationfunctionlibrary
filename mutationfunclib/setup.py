from setuptools import setup

setup(
    name='mutationfunclib',
    version='0.1',
    description='A small library I made to have mutation functions handy when I need them',
    author='Edward O Reilly',
    author_email='edwardjoreilly01@gmail.com',
    packages=['mutationfunclib'],  #same as the directory name
    install_requires=[] #list any dependencies your library needs
)